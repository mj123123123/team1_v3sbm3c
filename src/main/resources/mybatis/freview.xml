<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.mvc.freview.FreviewDAOInter">
	<!-- 등록한 레코드 갯수 1 리턴, #{contentsno}: public int getContentsno() 호출됨 -->
	<insert id="create" parameterType="dev.mvc.freview.FreviewVO">
		INSERT INTO freview(reviewno, fcateno, contentsno, memberno, title, content, pwd, rdate,
		file1, file1saved, thumb1, size1)
		VALUES(review_seq.nextval, #{fcateno}, #{contentsno}, #{memberno}, #{title}, #{content}, #{pwd},
		sysdate, #{file1}, #{file1saved}, #{thumb1}, #{size1})
	</insert>
	
</mapper>